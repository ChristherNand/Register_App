<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.csji.userregister">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdministrarClientesActivity"></activity>
        <activity android:name=".EditarActivity" />
        <activity
            android:name=".IngresarActivity"
            android:label="@string/ingresar_label" />
        <activity
            android:name=".ListaActivity"
            android:label="@string/lista_usuarios" />
        <activity
            android:name=".RegistroActivity"
            android:label="@string/registro" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>